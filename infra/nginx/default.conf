upstream localhost {
    server web:8000;
}

server {
    listen 80;
    server_name 127.0.0.1;
    location /static/ {
        root /var/html/;
    }
    location /media/ {
        root /var/html/;
    }
    location / {
        proxy_pass http://localhost;
    }
    server_tokens off;
}
